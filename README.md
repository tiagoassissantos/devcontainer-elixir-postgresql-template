A basic devcontainer template to use in GitHub Codespace.

- Based in **Arch Linux**
- **PostgreSQL**
- Using **asdf** as runtime version manager
- Install **Erlang**, **Elixir** and **NodeJS** with **asdf**
